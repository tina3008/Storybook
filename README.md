# React + TypeScript + Vite 


This repo contains three reusable components implemented with React + TypeScript and documented in Storybook:
- `Input` — multi-type input with password visibility toggle and clearable option
- `Toast` — bottom-right toast notifications with auto-dismiss and transitions
- `SidebarMenu` — slide-in from right with nested items and backdrop close